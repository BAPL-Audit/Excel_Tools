{
  "name": "audit-tools-dashboard",
  "version": "1.0.0",
  "description": "Professional audit tools dashboard with authentication and admin functionality",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install",
    "setup": "npm run install-deps && npm run init-db",
    "init-db": "cd server && node src/config/database.js"
  },
  "keywords": [
    "audit",
    "tools",
    "dashboard",
    "authentication",
    "admin",
    "react",
    "node",
    "mongodb"
  ],
  "author": "Audit Tools Dashboard",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}